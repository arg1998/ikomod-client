import 'package:flutter/material.dart';

class PostManagementScreen extends StatelessWidget {
  static const routeName = "/postManagement";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Post Management Screen"),
      ),
    );
  }
}
