import 'package:flutter/material.dart';

class PlansScreen extends StatelessWidget {
  static const routeName = "/plans";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Plans Screen"),
      ),
    );
  }
}
