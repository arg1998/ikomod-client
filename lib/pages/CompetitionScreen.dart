import 'package:flutter/material.dart';

class CompetitionScreen extends StatelessWidget {
  static const routeName = "/competition";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Competition Screen"),
      ),
    );
  }
}
